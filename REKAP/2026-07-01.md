# Rekap Perubahan — 1 Juli 2026

**Proyek:** SIAMIN / SIMAIK  
**Tanggal:** 1 Juli 2026

---

## Backend (siamin1.0)

### 1. Relasi TPK pada Tabel Peserta

| File | Perubahan |
|------|-----------|
| `2026_06_30_085143_add_id_tpk_to_peserta_table.php` | **BARU** — Migration: kolom `id_tpk` (nullable FK → `tpk.id_tpk`) |
| `Peserta.php` | `id_tpk` ke fillable + relasi `tpk()` |
| `Tpk.php` | Relasi `peserta()` |
| `PesertaController.php` | Eager-load `tpk`, validasi `id_tpk` di `store`/`update` |

### 2. Evaluasi Berdasarkan TPK

| File | Perubahan |
|------|-----------|
| `2026_06_30_234245_add_id_tpk_to_evaluasi_table.php` | **BARU** — Migration: kolom `id_tpk` (nullable FK → `tpk.id_tpk`) |
| `Evaluasi.php` | `id_tpk` ke fillable + relasi `tpk()` |
| `EvaluasiController.php` | Semua method mendukung filter `id_tpk` opsional |
| `routes/api.php` | Route evaluasi: `{id_kegiatan}/{id_tpk?}` |
| `API_EVALUASI.md` | Dokumentasi diperbarui |

### Route Evaluasi Baru

```
POST   /api/v1/evaluasi
GET    /api/v1/evaluasi/check/{id_kegiatan}/{id_tpk?}
GET    /api/v1/evaluasi/{id_kegiatan}/{id_tpk?}/statistik
GET    /api/v1/evaluasi/{id_kegiatan}/{id_tpk?}
```

---

## Frontend (siamin-ui)

### 1. TPK pada Form Peserta

| File | Perubahan |
|------|-----------|
| `FormulirPeserta.vue` | Dropdown TPK berdasarkan kegiatan |
| `PesertaManagement.vue` | Dropdown TPK di admin + kolom TPK di tabel + filter TPK + biodata TPK |
| `DaftarPesertaPublik.vue` | Kolom TPK di tabel publik |

### 2. Evaluasi Berdasarkan TPK

| File | Perubahan |
|------|-----------|
| `router/index.js` | Route: `/evaluasi/:kode/:idTpk?/:slugJudul?` |
| `EvaluasiKegiatan.vue` | Filter narasumber by TPK, kirim `id_tpk` |
| `LaporanEvaluasi.vue` | Filter statistik by `id_tpk` |

### 3. Link Evaluasi Per-TPK

| File | Perubahan |
|------|-----------|
| `KegiatanDetailPublik.vue` | 1 link evaluasi per TPK + QR code per TPK |
| `Landing.vue` | 1 link evaluasi per TPK + QR code per TPK |
| `Dashboard.vue` | 1 link evaluasi per TPK + QR code per TPK |
| `Kegiatan.vue` | `activityLinks` menghasilkan 1 entry per TPK |

### Contoh Output Link Evaluasi

```
Hotel Lombok Raya (Kota Mataram)
http://localhost:5173/evaluasi/6/1/konsolidasi-daerah-...

Hotel Lombok Garden (Kota Mataram)
http://localhost:5173/evaluasi/6/8/konsolidasi-daerah-...
```

---

## Total File Diubah

| Lingkup | Jumlah File |
|---------|-------------|
| Backend (baru + modified) | 9 file |
| Frontend (modified) | 10 file |
| **Total** | **19 file** |

---

*Dokumen ini dibuat otomatis pada 1 Juli 2026.*
